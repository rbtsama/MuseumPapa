{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "buildCommand": "cd coupon-map && export COREPACK_ENABLE_DOWNLOAD_PROMPT=0 && corepack enable && pnpm install --no-frozen-lockfile && pnpm run build",
  "outputDirectory": "coupon-map/dist",
  "installCommand": "echo 'skip — handled inside buildCommand'",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
