# Execution Backlog — 2026-05-27

## Completed In This Slice

- [x] Add regression coverage for `Malden -> BPL` cross-consortium booking access.
- [x] Extend `library` build output with:
  - `consortium_label`
  - `card_issuance_group`
  - `card_issuance_groups`
  - `card_auth_groups`
- [x] Seed `BPL` with `card_auth_groups = ["BPL", "MBLN"]`.
- [x] Replace admin panel L1 card check so it uses access-group overlap instead of raw `network === network`.
- [x] Replace shared web eligibility L1 card check with the same access-group logic.
- [x] Rebuild `data/structured/*` so runtime data actually carries the new fields.
- [x] Fix `BPL/LibCal` `pass_form` recovery when catalog slug and index slug differ.
- [x] Add `detail_url -> museum_hex` fallback so BPL entries with shortened index slugs still recover the correct `pass_type`.
- [x] Tighten admin panel so warned/unknown eligibility no longer appears as clean confirmed access.
- [x] Add offline `pass_form` validation against local LibCal catalog text.
- [x] Expose optional `source_url` liveness validation via injected fetcher.
- [x] Wire offline pass-form conflict reporting into `scripts/build_all.py`.
- [x] Add cached real-fetch `source_url` validation with TTL and transient-failure skip.
- [x] Replace `testgame` card-coverage assumptions so it uses `card_auth_groups`, not raw `network`.
- [x] Add validator checks for booking-probe vs `requires_own_card` contradictions.
- [x] Preserve structured `booking_access_probe` verdicts in `passes.json` (`own_card_only` / `network_open` / `not_verified`).
- [x] Teach admin/web eligibility logic to prefer `booking_access_probe.verdict` over guessing from `requires_own_card`.
- [x] Add `ambiguous` booking-probe state end-to-end (probe script → build → panel/web warnings).
- [x] Confirm `web/public/admin/*` is generated by `web/sync-admin.mjs`, then resync deploy assets from `admin/*`.

## Next Tasks

- [ ] Split `pass_form` semantics more explicitly for LibCal/BPL:
  - `digital_email`
  - `physical_coupon`
  - `physical_circ`
  - pickup-without-return cases must not collapse into `physical_circ`
- [ ] Add validator checks for:
- [ ] Run a fresh Assabet probe batch so real `ambiguous` rows are populated in `data/raw/assabet/residency_probe/*`.

## Notes

- This slice intentionally fixes the highest-confidence, audit-proven bug first:
  current single-`network` modeling cannot represent real `BPL`/`MBLN` coverage.
- Larger booking-access refactors should build on these new library-level fields,
  not add more one-off overrides into panel logic.
- First live `source_url` sweep is now resumable via disk cache, but a full 1002-URL run still exceeds a single 15-minute window.
- Partial live cache already surfaced 4 concrete 404s, all on `new-england-aquarium` Assabet detail URLs:
  - `actonmemoriallibrary`
  - `belmontpubliclibrary`
  - `everettpubliclibraries`
  - `framinghamlibrary`
- `web/public/admin/*` is not a second source tree; it is a generated deploy snapshot and must be updated via `pnpm run copy:admin`.
