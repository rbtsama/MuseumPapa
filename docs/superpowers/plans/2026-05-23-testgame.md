# testgame.html Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 生成根目录单文件 `testgame.html` — 用真实抽样数据（9 馆 / 5 联盟 / 7 景点）演示 museum-pass 预定的 4 态交互。

**Architecture:** Python 抽取层从 `data/structured/*` 选出真实子集并渲染成自包含 HTML；纯 JS 决策函数 `classifyAttraction` 单独成 ESM 以便 `node --test` 单测，构建时去掉 `export` 内联进 HTML。逻辑（可测）与 UI（模板）分离。

**Tech Stack:** Python 3.11+ + pytest 9（`python -m pytest`）；Node v22 内置 `node --test`（零依赖）；前端为原生 HTML/CSS/JS（无框架，双击即开）。

设计依据：`docs/superpowers/specs/2026-05-23-testgame-design.md`。

固定抽样（真实，全部存在于 `data/structured/*`）：
- 9 馆：`lynn lynnfield belmont acton chelmsford andover malden quincy lexington`
- 7 景点：`new-england-aquarium museum-of-science isabella-stewart-gardner-museum zoo-new-england boston-childrens-museum peabody-essex-museum blithewold`
- 范围外镇：`Salem`、`Worcester`

---

## File Structure

| 文件 | 职责 |
|---|---|
| `src/malibbene/testgame/__init__.py` | 包标记（空） |
| `src/malibbene/testgame/select.py` | 纯函数 `select_sample(libraries, attractions, passes) -> dict`：选子集、规范成前端数据形状，不编造 |
| `src/malibbene/testgame/logic.mjs` | 纯 ESM `classifyAttraction(...)`：4 态决策，无 DOM 依赖 |
| `src/malibbene/testgame/logic.test.mjs` | `node --test` 单测，覆盖全 4 态 |
| `src/malibbene/testgame/template.html` | UI 外壳（HTML/CSS/JS），含 `/*__LOGIC__*/` 与 `/*__DATA__*/` 两个注入点 |
| `src/malibbene/testgame/render.py` | `render_html(sample, logic_js, template) -> str`：注入数据 + 去 export 内联逻辑 |
| `scripts/build_testgame.py` | CLI：读 data/structured/* → select → render → 写 `./testgame.html` |
| `tests/test_testgame.py` | pytest：select 真实性/覆盖性 + render 注入正确性 |

---

## Task 1: JS 决策逻辑 `classifyAttraction` + node 单测

**Files:**
- Create: `src/malibbene/testgame/logic.mjs`
- Test: `src/malibbene/testgame/logic.test.mjs`

数据形状约定（select.py 产出、模板内 `TESTGAME_DATA`、本函数消费的 pass 对象）：
`{ library_id, attraction_slug, network, library_name, library_town, residency('yes'|'no'|'unknown'), scope, summary(string|null) }`

- [ ] **Step 1: 写失败的测试**

`src/malibbene/testgame/logic.test.mjs`：

```js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { classifyAttraction } from './logic.mjs';

// 真实抽样的最小子集
const DATA = {
  passes: [
    { library_id:'lexington', attraction_slug:'blithewold', network:'Minuteman', library_name:'Cary Memorial Library', library_town:'Lexington', residency:'yes', scope:'town', summary:'50% off' },
    { library_id:'lynn', attraction_slug:'isabella-stewart-gardner-museum', network:'NOBLE', library_name:'Lynn Public Library', library_town:'Lynn', residency:'yes', scope:'town', summary:'$10/person' },
    { library_id:'lynnfield', attraction_slug:'isabella-stewart-gardner-museum', network:'NOBLE', library_name:'Lynnfield Public Library', library_town:'Lynnfield', residency:'no', scope:null, summary:'FREE' },
  ],
};

test('state 1: 持卡且居民 -> 完全符合', () => {
  const r = classifyAttraction('blithewold', DATA, ['lexington'], 'Lexington');
  assert.equal(r.state, 1);
  assert.deepEqual(r.tags, []);
  assert.equal(r.usableCards.length, 1);
  assert.equal(r.usableCards[0].library_id, 'lexington');
});

test('state 3: 持卡但非居民 -> Resident Only', () => {
  const r = classifyAttraction('blithewold', DATA, ['lexington'], 'Salem');
  assert.equal(r.state, 3);
  assert.deepEqual(r.tags, ['resident_only']);
  assert.deepEqual(r.residentOnlyTowns, ['Lexington']);
  assert.equal(r.usableCards.length, 1);
});

test('state 2: 居民但无卡 -> Library Pass Needed', () => {
  const r = classifyAttraction('blithewold', DATA, [], 'Lexington');
  assert.equal(r.state, 2);
  assert.deepEqual(r.tags, ['library_pass_needed']);
  assert.equal(r.recommendCards.some(c => c.library_id === 'lexington'), true);
});

test('state 4: 无卡且非居民（全 resident-only 景点）-> 双警告', () => {
  const r = classifyAttraction('blithewold', DATA, [], 'Salem');
  assert.equal(r.state, 4);
  assert.deepEqual(r.tags, ['resident_only', 'library_pass_needed']);
});

test('state 2: 无卡但有 open pass 兜底 -> 仅缺卡', () => {
  const r = classifyAttraction('isabella-stewart-gardner-museum', DATA, [], 'Salem');
  assert.equal(r.state, 2);
  assert.equal(r.recommendCards.some(c => c.library_id === 'lynnfield'), true);
});

test('state 1: open 馆持卡直接可用', () => {
  const r = classifyAttraction('isabella-stewart-gardner-museum', DATA, ['lynnfield'], 'Salem');
  assert.equal(r.state, 1);
});

test('state 3: 持 resident-only 卡且非居民，open 馆未持有', () => {
  const r = classifyAttraction('isabella-stewart-gardner-museum', DATA, ['lynn'], 'Salem');
  assert.equal(r.state, 3);
  assert.deepEqual(r.residentOnlyTowns, ['Lynn']);
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node --test src/malibbene/testgame/logic.test.mjs`
Expected: FAIL — `Cannot find module './logic.mjs'` 或 `classifyAttraction is not a function`。

- [ ] **Step 3: 写最小实现**

`src/malibbene/testgame/logic.mjs`：

```js
// 纯决策逻辑：无 DOM 依赖。构建时会被剥掉 `export` 内联进 HTML。
export function classifyAttraction(attractionSlug, data, heldLibIds, homeTown) {
  const held = new Set(heldLibIds);
  const enriched = data.passes
    .filter((p) => p.attraction_slug === attractionSlug)
    .map((p) => ({
      ...p,
      held: held.has(p.library_id),
      residentOK: p.residency !== 'yes' || homeTown === p.library_town,
    }));

  const fullyUsable = enriched.filter((p) => p.held && p.residentOK);
  const heldPasses = enriched.filter((p) => p.held);
  const residentOKPasses = enriched.filter((p) => p.residentOK);

  let state;
  let tags;
  if (fullyUsable.length > 0) { state = 1; tags = []; }
  else if (heldPasses.length > 0) { state = 3; tags = ['resident_only']; }
  else if (residentOKPasses.length > 0) { state = 2; tags = ['library_pass_needed']; }
  else { state = 4; tags = ['resident_only', 'library_pass_needed']; }

  const uniqLibs = (arr) => {
    const seen = new Set();
    const out = [];
    for (const p of arr) {
      if (seen.has(p.library_id)) continue;
      seen.add(p.library_id);
      out.push({ library_id: p.library_id, library_name: p.library_name, network: p.network, library_town: p.library_town });
    }
    return out;
  };

  return {
    state,
    tags,
    passes: enriched,
    usableCards: uniqLibs(state === 1 ? fullyUsable : heldPasses),
    recommendCards: uniqLibs(residentOKPasses),
    offeringCards: uniqLibs(enriched),
    residentOnlyTowns: [...new Set(heldPasses.filter((p) => p.residency === 'yes').map((p) => p.library_town))],
  };
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `node --test src/malibbene/testgame/logic.test.mjs`
Expected: PASS — 7 tests passed。

- [ ] **Step 5: 提交**

```bash
git add src/malibbene/testgame/logic.mjs src/malibbene/testgame/logic.test.mjs
git commit -m "feat(testgame): classifyAttraction 4-state decision logic + node tests"
```

---

## Task 2: Python 抽样选择 `select_sample` + pytest

**Files:**
- Create: `src/malibbene/testgame/__init__.py`（空文件）
- Create: `src/malibbene/testgame/select.py`
- Test: `tests/test_testgame.py`

- [ ] **Step 1: 写失败的测试**

`tests/test_testgame.py`：

```python
import json
from pathlib import Path

from malibbene.testgame.select import (
    select_sample,
    SAMPLE_LIB_IDS,
    SAMPLE_ATTRACTION_SLUGS,
    EXTRA_TOWNS,
)

REPO = Path(__file__).resolve().parents[1]
STRUCT = REPO / "data" / "structured"


def _load():
    libs = json.loads((STRUCT / "libraries.json").read_text(encoding="utf-8"))["libraries"]
    attrs = json.loads((STRUCT / "attractions.json").read_text(encoding="utf-8"))["attractions"]
    passes = json.loads((STRUCT / "passes.json").read_text(encoding="utf-8"))["passes"]
    return libs, attrs, passes


def test_sample_sizes_and_real():
    sample = select_sample(*_load())
    assert len(sample["libraries"]) == len(SAMPLE_LIB_IDS) == 9
    assert len(sample["attractions"]) == len(SAMPLE_ATTRACTION_SLUGS) == 7
    # 全部 id/slug 真实命中（select 内部已 assert，这里再确认形状）
    assert {l["id"] for l in sample["libraries"]} == set(SAMPLE_LIB_IDS)
    assert {a["slug"] for a in sample["attractions"]} == set(SAMPLE_ATTRACTION_SLUGS)


def test_networks_span_five():
    sample = select_sample(*_load())
    nets = {l["network"] for l in sample["libraries"]}
    assert {"NOBLE", "Minuteman", "MVLC", "MBLN", "OCLN"} <= nets


def test_towns_include_in_and_out_of_range():
    sample = select_sample(*_load())
    assert set(EXTRA_TOWNS) <= set(sample["towns"])
    assert "Lexington" in sample["towns"]
    # 范围外镇不是任何样本馆所在镇
    lib_towns = {l["town"] for l in sample["libraries"]}
    assert all(t not in lib_towns for t in EXTRA_TOWNS)


def test_blithewold_is_state4_anchor():
    sample = select_sample(*_load())
    blith = [p for p in sample["passes"] if p["attraction_slug"] == "blithewold"]
    assert len(blith) == 1
    assert blith[0]["library_id"] == "lexington"
    assert blith[0]["residency"] == "yes"


def test_no_fabrication_summary_passthrough():
    sample = select_sample(*_load())
    # summary 要么为 None 要么是非空字符串，绝不补默认值
    for p in sample["passes"]:
        assert p["summary"] is None or (isinstance(p["summary"], str) and p["summary"])
        assert p["residency"] in {"yes", "no", "unknown"}


def test_pass_records_have_required_fields():
    sample = select_sample(*_load())
    assert sample["passes"], "应至少有若干 pass"
    for p in sample["passes"]:
        for k in ("library_id", "attraction_slug", "network", "library_name", "library_town", "residency", "summary"):
            assert k in p
        # 只保留样本馆 × 样本景点的 cell
        assert p["library_id"] in SAMPLE_LIB_IDS
        assert p["attraction_slug"] in SAMPLE_ATTRACTION_SLUGS
```

- [ ] **Step 2: 跑测试确认失败**

Run: `python -m pytest tests/test_testgame.py -q`
Expected: FAIL — `ModuleNotFoundError: No module named 'malibbene.testgame'`。

- [ ] **Step 3: 写实现**

`src/malibbene/testgame/__init__.py`：留空文件。

`src/malibbene/testgame/select.py`：

```python
"""从 data/structured/* 选出 testgame 的真实抽样子集并规范成前端数据形状。

铁律：只搬运真实数据，绝不补值。coupon 缺失 -> summary=None；residency 原样透传。
"""
from __future__ import annotations

# 9 馆，跨 NOBLE/Minuteman/MVLC/MBLN/OCLN 5 联盟，居民/开放/未标注混合
SAMPLE_LIB_IDS = [
    "lynn", "lynnfield", "belmont", "acton",
    "chelmsford", "andover", "malden", "quincy", "lexington",
]
# 7 景点；blithewold 是全库唯一“仅 resident-only 馆提供”的景点 -> 状态 4 锚点
SAMPLE_ATTRACTION_SLUGS = [
    "new-england-aquarium", "museum-of-science", "isabella-stewart-gardner-museum",
    "zoo-new-england", "boston-childrens-museum", "peabody-essex-museum", "blithewold",
]
# 抽样范围外的镇，用作非居民 Home Town
EXTRA_TOWNS = ["Salem", "Worcester"]


def _adult_price(attraction: dict):
    for pr in attraction.get("prices") or []:
        if pr.get("audience") == "adult" and pr.get("price") is not None:
            return pr["price"]
    return None


def select_sample(libraries: list[dict], attractions: list[dict], passes: list[dict]) -> dict:
    lib_by_id = {l["id"]: l for l in libraries}
    attr_by_slug = {a["slug"]: a for a in attractions}

    missing_libs = [i for i in SAMPLE_LIB_IDS if i not in lib_by_id]
    missing_attrs = [s for s in SAMPLE_ATTRACTION_SLUGS if s not in attr_by_slug]
    if missing_libs or missing_attrs:
        raise ValueError(f"样本不在数据集中: libs={missing_libs} attractions={missing_attrs}")

    out_libs = [
        {"id": l["id"], "name": l["name"], "town": l["town"], "network": l["network"]}
        for l in (lib_by_id[i] for i in SAMPLE_LIB_IDS)
    ]
    out_attrs = []
    for s in SAMPLE_ATTRACTION_SLUGS:
        a = attr_by_slug[s]
        booking = (a.get("reservation") or {}).get("booking_url")
        out_attrs.append({
            "slug": a["slug"],
            "name": a["name"],
            "website": a.get("website"),
            "booking_url": booking or a.get("website"),
            "price_adult": _adult_price(a),
        })

    lib_set = set(SAMPLE_LIB_IDS)
    attr_set = set(SAMPLE_ATTRACTION_SLUGS)
    out_passes = []
    for p in passes:
        if p["library_id"] not in lib_set or p["attraction_slug"] not in attr_set:
            continue
        l = lib_by_id[p["library_id"]]
        rr = p.get("residency_restriction") or {}
        coupon = p.get("coupon") or {}
        out_passes.append({
            "library_id": p["library_id"],
            "attraction_slug": p["attraction_slug"],
            "network": l["network"],
            "library_name": l["name"],
            "library_town": l["town"],
            "residency": rr.get("restricted", "unknown"),
            "scope": rr.get("scope"),
            "summary": coupon.get("summary"),  # 可能为 None，原样保留
        })

    towns = sorted({l["town"] for l in out_libs}) + list(EXTRA_TOWNS)
    return {"towns": towns, "libraries": out_libs, "attractions": out_attrs, "passes": out_passes}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `python -m pytest tests/test_testgame.py -q`
Expected: PASS — 6 passed。

- [ ] **Step 5: 提交**

```bash
git add src/malibbene/testgame/__init__.py src/malibbene/testgame/select.py tests/test_testgame.py
git commit -m "feat(testgame): real-data sample selection (9 libs / 5 networks / 7 attractions) + pytest"
```

---

## Task 3: UI 模板 + 渲染注入 `render_html` + pytest

**Files:**
- Create: `src/malibbene/testgame/template.html`
- Create: `src/malibbene/testgame/render.py`
- Modify: `tests/test_testgame.py`（追加 render 测试）

- [ ] **Step 1: 追加失败的测试**

在 `tests/test_testgame.py` 末尾追加：

```python
from malibbene.testgame.render import render_html


def test_render_injects_data_and_logic_no_export():
    sample = select_sample(*_load())
    logic = "export function classifyAttraction(a, b, c, d) { return {state:1}; }"
    template = "<html><script>/*__LOGIC__*/</script><script>/*__DATA__*/\nrender();</script></html>"
    html = render_html(sample, logic, template)
    assert "const TESTGAME_DATA =" in html
    assert "function classifyAttraction" in html
    # 内联后不得残留 ESM export（否则 classic <script> 报错）
    assert "export function" not in html
    assert "/*__LOGIC__*/" not in html and "/*__DATA__*/" not in html
    # 真实景点名进入产物
    assert "Blithewold" in html


def test_render_with_real_template_and_logic():
    from pathlib import Path
    pkg = REPO / "src" / "malibbene" / "testgame"
    template = (pkg / "template.html").read_text(encoding="utf-8")
    logic = (pkg / "logic.mjs").read_text(encoding="utf-8")
    sample = select_sample(*_load())
    html = render_html(sample, logic, template)
    assert "export" not in html.split("</script>")[0] or "export function" not in html
    assert "classifyAttraction" in html
    assert "TESTGAME_DATA" in html
```

- [ ] **Step 2: 跑测试确认失败**

Run: `python -m pytest tests/test_testgame.py -q`
Expected: FAIL — `ModuleNotFoundError: No module named 'malibbene.testgame.render'`。

- [ ] **Step 3a: 写模板 `src/malibbene/testgame/template.html`**

```html
<!DOCTYPE html>
<html lang="zh">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>Museum Pass — 预定交互沙盒 (testgame)</title>
<style>
  :root { --bg:#f6f7f9; --card:#fff; --line:#e3e6ea; --ink:#1c2430; --muted:#6b7480; --accent:#2563eb; }
  * { box-sizing:border-box; }
  body { margin:0; font:15px/1.5 system-ui,"Segoe UI",sans-serif; background:var(--bg); color:var(--ink); }
  header { padding:18px 20px; background:#0f172a; color:#fff; }
  header h1 { margin:0; font-size:18px; }
  header p { margin:4px 0 0; font-size:13px; color:#9fb0c7; }
  main { max-width:980px; margin:0 auto; padding:20px; }
  .panel { background:var(--card); border:1px solid var(--line); border-radius:12px; padding:16px; margin-bottom:20px; }
  .panel h2 { margin:0 0 12px; font-size:15px; }
  select { padding:8px 10px; border:1px solid var(--line); border-radius:8px; font:inherit; min-width:240px; }
  #libraries { display:grid; grid-template-columns:1fr 1fr; gap:8px; margin-top:12px; }
  label.lib { display:flex; align-items:center; gap:8px; padding:8px; border:1px solid var(--line); border-radius:8px; }
  label.lib > span { flex:1; }
  .cardnum { width:120px; padding:5px 7px; border:1px solid var(--line); border-radius:6px; font:inherit; }
  .cardnum:disabled { background:#f0f1f3; }
  .badge { display:inline-block; font-size:11px; padding:1px 6px; border-radius:999px; background:#eef2ff; color:#3949ab; }
  small { color:var(--muted); }
  .card { background:var(--card); border:1px solid var(--line); border-radius:12px; padding:16px; margin-bottom:16px; }
  .card-head { display:flex; align-items:baseline; gap:12px; }
  .card-head h3 { margin:0; font-size:16px; flex:1; }
  .price { color:var(--muted); font-size:13px; }
  .passes { margin:12px 0; border-top:1px dashed var(--line); }
  .passrow { display:flex; align-items:center; gap:10px; padding:7px 0; border-bottom:1px dashed var(--line); font-size:14px; }
  .passrow .sum { width:120px; font-weight:600; }
  .passrow .lib2 { flex:1; }
  .res { font-size:12px; padding:2px 8px; border-radius:999px; }
  .res-yes { background:#fef2f2; color:#b91c1c; }
  .res-open { background:#ecfdf5; color:#047857; }
  .res-unknown { background:#f3f4f6; color:#6b7280; }
  .card-foot { display:flex; align-items:center; gap:10px; }
  .tags { flex:1; display:flex; gap:6px; }
  .tag { font-size:12px; padding:3px 9px; border-radius:999px; font-weight:600; }
  .tag-res { background:#fef2f2; color:#b91c1c; }
  .tag-card { background:#fff7ed; color:#c2410c; }
  button { font:inherit; cursor:pointer; border:1px solid var(--line); background:#fff; padding:8px 14px; border-radius:8px; }
  button.book, button.primary { background:var(--accent); color:#fff; border-color:var(--accent); font-weight:600; }
  button:disabled { opacity:.5; cursor:not-allowed; }
  .modal { position:fixed; inset:0; background:rgba(15,23,42,.5); display:flex; align-items:center; justify-content:center; padding:20px; }
  .modal.hidden { display:none; }
  .modal-box { background:#fff; border-radius:14px; padding:22px; max-width:460px; width:100%; position:relative; }
  .modal-close { position:absolute; top:10px; right:12px; border:none; background:none; font-size:22px; line-height:1; }
  .modal-box h2 { margin:0 0 12px; font-size:17px; }
  .modal-box h4 { margin:14px 0 6px; font-size:13px; color:var(--muted); }
  .warn { background:#fffbeb; border:1px solid #fde68a; color:#92400e; padding:9px 11px; border-radius:8px; margin:6px 0; font-size:13px; }
  .cardline { display:flex; align-items:center; gap:8px; padding:6px 0; border-bottom:1px dashed var(--line); font-size:14px; }
  .cardline span { flex:1; }
  .cardline code { background:#f3f4f6; padding:2px 6px; border-radius:5px; }
  #modal-actions { margin-top:16px; display:flex; justify-content:flex-end; gap:8px; }
</style>
</head>
<body>
<header>
  <h1>Museum Pass — 预定交互沙盒</h1>
  <p>真实抽样数据 · 选 Home Town + 勾选你“持有”的图书馆卡 · 点 Book 看 4 态引导</p>
</header>
<main>
  <div class="panel">
    <h2>① 你的 Home Town</h2>
    <select id="town"></select>
  </div>
  <div class="panel">
    <h2>② 你持有的图书馆卡（多选，勾选后可手填卡号）</h2>
    <div id="libraries"></div>
  </div>
  <div id="attractions"></div>
</main>

<div id="modal" class="modal hidden">
  <div class="modal-box">
    <button class="modal-close" aria-label="close">&times;</button>
    <h2 id="modal-title"></h2>
    <div id="modal-body"></div>
    <div id="modal-actions"></div>
  </div>
</div>

<script>
/*__LOGIC__*/
</script>
<script>
/*__DATA__*/

const state = { homeTown: '', held: new Set(), cardNumbers: {} };
const $ = (sel) => document.querySelector(sel);

const networkBadge = (net) => `<span class="badge">${net}</span>`;
function residencyLabel(p) {
  if (p.residency === 'yes') return `<span class="res res-yes">Resident Only · ${p.library_town}</span>`;
  if (p.residency === 'no') return `<span class="res res-open">Open</span>`;
  return `<span class="res res-unknown">Not stated</span>`;
}
const summaryText = (p) => (p.summary ? p.summary : '<em>无折扣信息</em>');

function renderControls() {
  const opts = ['<option value="">— 选择你的 Home Town —</option>']
    .concat(TESTGAME_DATA.towns.map((t) => `<option value="${t}">${t}</option>`)).join('');
  $('#town').innerHTML = opts;
  $('#town').value = state.homeTown;
  $('#libraries').innerHTML = TESTGAME_DATA.libraries.map((l) => `
    <label class="lib">
      <input type="checkbox" data-lib="${l.id}" ${state.held.has(l.id) ? 'checked' : ''}>
      <span>${l.name} ${networkBadge(l.network)} <small>${l.town}</small></span>
      <input type="text" class="cardnum" data-card="${l.id}" placeholder="卡号(可选)"
        value="${state.cardNumbers[l.id] || ''}" ${state.held.has(l.id) ? '' : 'disabled'}>
    </label>`).join('');
}

function renderAttractions() {
  $('#attractions').innerHTML = TESTGAME_DATA.attractions.map((a) => {
    const r = classifyAttraction(a.slug, TESTGAME_DATA, [...state.held], state.homeTown);
    const rows = r.passes.map((p) => `
      <div class="passrow">
        <span class="sum">${summaryText(p)}</span>
        <span class="lib2">${p.library_name} ${networkBadge(p.network)}</span>
        ${residencyLabel(p)}
      </div>`).join('') || '<div class="passrow"><em>样本馆均不提供此景点 pass</em></div>';
    const tagHtml = r.tags.map((t) => (t === 'resident_only'
      ? '<span class="tag tag-res">Resident Only</span>'
      : '<span class="tag tag-card">Library Pass Needed</span>')).join('');
    const price = a.price_adult != null ? `成人 $${a.price_adult}` : '价格未知';
    return `
      <div class="card">
        <div class="card-head">
          <h3>${a.name}</h3>
          <a href="${a.website || '#'}" target="_blank" rel="noopener">官网</a>
          <span class="price">${price}</span>
        </div>
        <div class="passes">${rows}</div>
        <div class="card-foot">
          <div class="tags">${tagHtml}</div>
          <button class="book" data-slug="${a.slug}">Book</button>
        </div>
      </div>`;
  }).join('');
}

function render() { renderControls(); renderAttractions(); }

document.addEventListener('change', (e) => {
  if (e.target.matches('#town')) { state.homeTown = e.target.value; render(); }
  else if (e.target.matches('[data-lib]')) {
    const id = e.target.dataset.lib;
    if (e.target.checked) state.held.add(id); else state.held.delete(id);
    render();
  }
});
document.addEventListener('input', (e) => {
  if (e.target.matches('[data-card]')) state.cardNumbers[e.target.dataset.card] = e.target.value;
});
document.addEventListener('click', (e) => {
  if (e.target.matches('.book')) openModal(e.target.dataset.slug);
  else if (e.target.matches('.modal-close') || e.target.matches('#modal')) closeModal();
  else if (e.target.matches('[data-copy]')) {
    const v = e.target.dataset.copy;
    if (v && navigator.clipboard) navigator.clipboard.writeText(v);
    e.target.textContent = 'Copied';
  }
});

const go = (url) => window.open(url || '#', '_blank', 'noopener');

function openModal(slug) {
  const a = TESTGAME_DATA.attractions.find((x) => x.slug === slug);
  const r = classifyAttraction(slug, TESTGAME_DATA, [...state.held], state.homeTown);
  const url = a.booking_url || a.website || '#';

  let warn = '';
  if (r.tags.includes('library_pass_needed')) {
    warn += '<p class="warn">⚠ 需要图书馆卡：你当前未持有可领取此 Pass 的图书馆卡。</p>';
  }
  if (r.tags.includes('resident_only')) {
    const towns = r.state === 3 ? r.residentOnlyTowns : [...new Set(r.offeringCards.map((c) => c.library_town))];
    warn += `<p class="warn">⚠ 仅限居民：此活动仅限 ${towns.join('、')} 的 Resident 参加。</p>`;
  }

  let cards = '';
  if (r.usableCards.length) {
    cards = '<h4>你持有的可用卡</h4>' + r.usableCards.map((c) => {
      const num = state.cardNumbers[c.library_id] || '';
      return `<div class="cardline"><span>${c.library_name} ${networkBadge(c.network)}</span>
        <code>${num || '(未录入卡号)'}</code>
        <button data-copy="${num}" ${num ? '' : 'disabled'}>Copy</button></div>`;
    }).join('');
  } else if (r.state === 2) {
    cards = '<h4>需要以下任一图书馆的卡</h4>' + r.recommendCards.map((c) =>
      `<div class="cardline"><span>${c.library_name} ${networkBadge(c.network)} <small>${c.library_town}</small></span></div>`).join('');
  } else if (r.state === 4) {
    cards = '<h4>提供此景点的馆（均限居民）</h4>' + r.offeringCards.map((c) =>
      `<div class="cardline"><span>${c.library_name} ${networkBadge(c.network)} <small>${c.library_town}</small></span></div>`).join('');
  }

  const single = r.usableCards.length === 1 ? r.usableCards[0] : null;
  const singleNum = single ? (state.cardNumbers[single.library_id] || '') : '';
  let actions;
  if (single && singleNum) actions = '<button class="primary" id="copygo">Copy and Go</button>';
  else actions = `<button class="primary" id="goonly">${r.state === 1 ? 'Go' : 'Go anyway'}</button>`;

  $('#modal-title').textContent = a.name + (r.state === 1 ? ' — 可预定' : ' — 请注意');
  $('#modal-body').innerHTML = warn + cards;
  $('#modal-actions').innerHTML = actions;
  if ($('#copygo')) $('#copygo').onclick = () => { if (navigator.clipboard) navigator.clipboard.writeText(singleNum); go(url); };
  if ($('#goonly')) $('#goonly').onclick = () => go(url);
  $('#modal').classList.remove('hidden');
}
function closeModal() { $('#modal').classList.add('hidden'); }

render();
</script>
</body>
</html>
```

- [ ] **Step 3b: 写 `src/malibbene/testgame/render.py`**

```python
"""把抽样数据 + 决策逻辑注入模板，产出自包含 HTML。"""
from __future__ import annotations

import json


def _strip_exports(logic_js: str) -> str:
    # logic.mjs 用 `export function ...`；classic <script> 内联需去掉 export 关键字
    return logic_js.replace("export function", "function").replace("export default ", "")


def render_html(sample: dict, logic_js: str, template: str) -> str:
    data_js = "const TESTGAME_DATA = " + json.dumps(sample, ensure_ascii=False, indent=2) + ";"
    logic_inline = _strip_exports(logic_js)
    if "export function" in logic_inline:
        raise ValueError("内联逻辑仍残留 ESM export，会导致 <script> 解析失败")
    html = template.replace("/*__LOGIC__*/", logic_inline).replace("/*__DATA__*/", data_js)
    if "/*__LOGIC__*/" in html or "/*__DATA__*/" in html:
        raise ValueError("模板注入点未全部替换")
    return html
```

- [ ] **Step 4: 跑测试确认通过**

Run: `python -m pytest tests/test_testgame.py -q`
Expected: PASS — 8 passed（含新增 2 个 render 测试）。

- [ ] **Step 5: 提交**

```bash
git add src/malibbene/testgame/template.html src/malibbene/testgame/render.py tests/test_testgame.py
git commit -m "feat(testgame): UI template + render_html injection (data + de-export'd logic)"
```

---

## Task 4: 构建 CLI + 生成 `testgame.html`

**Files:**
- Create: `scripts/build_testgame.py`
- Create: `testgame.html`（构建产物，根目录）

- [ ] **Step 1: 写构建脚本 `scripts/build_testgame.py`**

```python
"""构建 testgame.html：读 data/structured/* -> 抽样 -> 渲染 -> 写根目录。"""
from __future__ import annotations

import json
import sys
from pathlib import Path

REPO = Path(__file__).resolve().parents[1]
sys.path.insert(0, str(REPO / "src"))

from malibbene.testgame.select import select_sample  # noqa: E402
from malibbene.testgame.render import render_html  # noqa: E402

STRUCT = REPO / "data" / "structured"
PKG = REPO / "src" / "malibbene" / "testgame"


def main() -> None:
    libs = json.loads((STRUCT / "libraries.json").read_text(encoding="utf-8"))["libraries"]
    attrs = json.loads((STRUCT / "attractions.json").read_text(encoding="utf-8"))["attractions"]
    passes = json.loads((STRUCT / "passes.json").read_text(encoding="utf-8"))["passes"]

    sample = select_sample(libs, attrs, passes)
    template = (PKG / "template.html").read_text(encoding="utf-8")
    logic = (PKG / "logic.mjs").read_text(encoding="utf-8")
    html = render_html(sample, logic, template)

    out = REPO / "testgame.html"
    out.write_text(html, encoding="utf-8")
    print(f"wrote {out} ({len(html)} bytes); "
          f"libs={len(sample['libraries'])} attractions={len(sample['attractions'])} passes={len(sample['passes'])}")


if __name__ == "__main__":
    main()
```

- [ ] **Step 2: 运行构建**

Run: `python scripts/build_testgame.py`
Expected: 打印 `wrote .../testgame.html (NNNNN bytes); libs=9 attractions=7 passes=46`（passes 数以实际为准，应为 40+）。

- [ ] **Step 3: 产物冒烟校验（grep 断言真实数据进入产物，无残留注入点 / export）**

Run（PowerShell）：
```powershell
$h = Get-Content -Raw testgame.html
@(
  ($h -match 'const TESTGAME_DATA ='),
  ($h -match 'function classifyAttraction'),
  ($h -notmatch 'export function'),
  ($h -notmatch '/\*__DATA__\*/'),
  ($h -match 'Blithewold'),
  ($h -match 'Cary Memorial Library'),
  ($h -match '"network": "OCLN"')
) -join ' '
```
Expected: `True True True True True True True`

- [ ] **Step 4: 提交**

```bash
git add scripts/build_testgame.py testgame.html
git commit -m "feat(testgame): build CLI + generated self-contained testgame.html"
```

---

## Task 5: 浏览器手动验证 4 态

**Files:** 无（仅验证；如发现 bug，回到对应 Task 修复并重跑构建）

- [ ] **Step 1: 在浏览器打开**

Run: `start testgame.html`（或双击根目录 `testgame.html`）。

- [ ] **Step 2: 逐态核对（用 blithewold 卡片）**

| 操作 | 期望 |
|---|---|
| Home Town=Lexington，勾选 Cary Memorial Library（Lexington）| blithewold 无标签；点 Book → “可预定”，1 张卡 → `Copy and Go` |
| Home Town=Salem，勾选 Lexington | blithewold 标 `Resident Only`；点 Book → 警告“仅限 Lexington 的 Resident”，列出 Lexington 卡 + Copy，按钮 `Copy and Go`/`Go anyway` |
| Home Town=Lexington，不勾任何卡 | blithewold 标 `Library Pass Needed`；点 Book → 警告需要卡，列出 Cary Memorial Library，按钮 `Go anyway` |
| Home Town=Salem，不勾任何卡 | blithewold 标 `Resident Only`+`Library Pass Needed`；点 Book → 两条警告并列，按钮 `Go anyway` |

- [ ] **Step 3: 核对状态 1/2/3 的多卡与开放馆（用 isabella-stewart-gardner-museum）**

| 操作 | 期望 |
|---|---|
| Home Town=Salem，勾选 Lynnfield + Quincy（均非 resident-only）| 该卡片可用；点 Book → 多张可用卡，按钮 `Go` + 每张卡各自 Copy |
| 卡号留空时 | 弹窗 Copy 按钮置灰显示“(未录入卡号)”，`Go` 仍可点 |
| 手填某馆卡号后 | 该卡 Copy 可点；单卡场景按钮变 `Copy and Go` |

- [ ] **Step 4: 确认无报错**

打开浏览器 DevTools Console，确认无红色报错（尤其无 `classifyAttraction is not defined` / `Unexpected token export`）。

- [ ] **Step 5: 如有改动则重跑构建并提交**

若 Step 2-4 暴露问题：定位到 Task 1/3 修复 → `python scripts/build_testgame.py` 重新生成 → 重跑相关测试 → 提交。

---

## Self-Review

**Spec coverage：**
- §2 单文件自包含 → Task 3 template + Task 4 内联构建 ✅
- §3.1/3.2/3.4 9 馆 / 7 景点 / 11 镇 → Task 2 `SAMPLE_LIB_IDS`/`SAMPLE_ATTRACTION_SLUGS`/`EXTRA_TOWNS` + 测试 ✅
- §4 布局（控制区 + 景点卡 + Pass 行三项展示） → Task 3 template ✅
- §5 四态状态机 + 标签 + 弹窗 + 全态可跳 → Task 1 logic + Task 3 modal；状态 4 经 blithewold → Task 5 验证 ✅
- §6 勾选可选填卡号 / 1 卡 Copy and Go / 多卡 Go+逐卡 Copy / 未填置灰 → Task 3 modal + Task 5 验证 ✅
- §7 非目标（无后端、零卡号入库、无框架） → 卡号仅运行时手填、纯原生 JS ✅

**Placeholder scan：** 无 TBD/TODO；所有步骤含完整代码与命令。

**Type consistency：** `classifyAttraction(attractionSlug, data, heldLibIds, homeTown)` 返回 `{state, tags, passes, usableCards, recommendCards, offeringCards, residentOnlyTowns}` —— Task 1 定义、Task 3 模板消费的字段名一致；pass 字段 `library_id/attraction_slug/network/library_name/library_town/residency/scope/summary` 在 select.py 产出与 logic 消费两端一致。
